diff --git a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -93 +93,4 @@ option(GLSLANG_TESTS "Enable glslang testing" ${GLSLANG_TESTS_DEFAULT})
 option(GLSLANG_ENABLE_INSTALL "Enable glslang installation" ${GLSLANG_ENABLE_INSTALL_DEFAULT})
+# Shaderc's static closure does not export the linked SPIRV-Tools targets.
+set(GLSLANG_ENABLE_INSTALL OFF)
+set(GLSLANG_ENABLE_INSTALL OFF CACHE BOOL "" FORCE)
