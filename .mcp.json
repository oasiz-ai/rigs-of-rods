{
  "mcpServers": {
    "rorsmith": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--quiet",
        "--directory",
        "${CLAUDE_PROJECT_DIR:-.}/tools/mcp/rorsmith",
        "rorsmith"
      ],
      "env": {
        "RORSMITH_REPO_ROOT": "${CLAUDE_PROJECT_DIR:-.}"
      }
    }
  }
}
