--- a/RenderSystems/GL3Plus/src/GLSL/src/OgreGLSLMonolithicProgram.cpp
+++ b/RenderSystems/GL3Plus/src/GLSL/src/OgreGLSLMonolithicProgram.cpp
@@ -96,11 +96,10 @@ namespace Ogre {
 
         logObjectInfo( getCombinedName() + String(" GLSL link result : "), mGLProgramHandle );
 
-        if(glIsProgram(mGLProgramHandle))
-        {
-            OGRE_CHECK_GL_ERROR(glValidateProgram(mGLProgramHandle));
-        }
-        logObjectInfo( getCombinedName() + String(" GLSL validation result : "), mGLProgramHandle );
+        // Program validation depends on complete draw-time state, including
+        // sampler bindings and a compatible VAO. Neither is established while
+        // this program is being linked, so link status and its info log above
+        // are the appropriate diagnostics here.
 
         if(mLinked)
         {
